[project]
name = "latex-cv-2"
version = "1.2.0"
requires-python = ">=3.11"
dependencies = [
    "pillow==11.0.0",
    "PyYAML==6.0.2",
]

[project.optional-dependencies]
dev = [
    "ruff==0.15.15",
]
