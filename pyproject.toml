[project]
name = "latex-cv"
version = "1.2.0"
requires-python = ">=3.11"
dependencies = [
    "pillow==11.0.0",
    "PyYAML==6.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]

[tool.setuptools.package-data]
latex_cv = ["**/*.png", "**/*.tex"]
